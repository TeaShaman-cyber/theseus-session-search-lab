# Research lifecycle

```text
IDEA -> SPECIFIED -> RUNNING -> VERIFYING -> ACCEPTED | REJECTED | INCONCLUSIVE
```

An experiment begins as an Issue. Commits advancing an open research question reference that Issue with `refs #N`. Execution success is not scientific acceptance. Important results carry a receipt that names the exact source revision/artifact and the verification postcondition.
